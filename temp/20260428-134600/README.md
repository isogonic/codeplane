# Tetris

This folder contains a standalone Tetris implementation built with plain HTML, CSS, and JavaScript.

## Run

Open `index.html` in a browser and press:

- `Enter` to start or resume
- `R` to restart
- `Arrow` keys for movement/rotation
- `Space` for hard drop

The game is intentionally lightweight and works offline.
