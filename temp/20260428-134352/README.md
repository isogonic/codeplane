# Arcade Tetris

Open `index.html` in a browser to play.

- `←` / `→`: move piece
- `↑`: rotate
- `↓`: soft drop
- `Space`: hard drop
- `P`: pause / resume
- `R`: restart

This folder is named with the current timestamp format `YYYYMMDD-HHMMSS`.
