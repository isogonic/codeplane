# Tetris

Standalone Tetris built with plain HTML, CSS, and JavaScript.

## Run

- Open `index.html` in a browser.
- Controls:
  - `Enter` start / pause
  - `R` reset
  - `←` `→` move
  - `↑` rotate
  - `↓` soft drop
  - `Space` hard drop
