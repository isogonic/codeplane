# Tetris

Open `index.html` in a browser to play.

Controls:

- `←` / `→` move left/right
- `↓` soft drop (also increases score)
- `↑` rotate
- `Space` hard drop
- `P` pause/resume
- `R` restart
