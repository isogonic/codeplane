# Tetris Demo

Open `index.html` in a browser to play.

- `A`/`D` or `←`/`→`: move piece
- `W`/`↑`: rotate
- `S`/`↓`: soft drop
- `Space`: hard drop
- `P`: pause / resume
- `R` / `Enter` or `New Game`: restart
