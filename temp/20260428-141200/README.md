# Tetris - 20260428-141200

Simple browser Tetris clone placed in a timestamped `temp` folder.

- Open `index.html` in a browser to play.
- Controls:
  - Arrow keys or `A`/`D` to move
  - `W` or `↑` to rotate
  - `S` or `↓` to soft drop
  - `Space` for hard drop
  - `R` to restart while game over
