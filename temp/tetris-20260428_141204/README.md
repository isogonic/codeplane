# Tetris

This folder contains a standalone HTML/CSS/JS Tetris build.

- `index.html` → game UI and controls
- `app.js` → gameplay logic
- `style.css` → visuals

Open `index.html` directly in a browser to play.
