# Tetris

This folder contains a single-file browser version of Tetris.

- Open `index.html` directly in a browser.
- Controls: `A/D` or ←/→ to move, `W` or ↑ to rotate, `S` or ↓ to soft drop, `Space` for hard drop, `R` to restart.

Folder created with current timestamp: `temp/20260428-141109`.
