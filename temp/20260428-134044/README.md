# Neon Tetris

Open `index.html` in a browser to play.

Controls:

- `←` / `→` move piece
- `↑` rotate
- `↓` soft drop
- `Space` hard drop
- `P` pause/resume
- `R` restart

This folder name uses the current timestamp format `YYYYMMDD-HHMMSS`.
