---
title: CLI
description: أوامر وخيارات CLI في CodePlane.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

يبدأ CLI الخاص بـ CodePlane افتراضيا واجهة [TUI](/docs/tui) عند تشغيله بدون أي معاملات.

```bash
codeplane
```

كما يدعم أيضا أوامر موثقة في هذه الصفحة، ما يتيح لك التفاعل مع CodePlane برمجيا.

```bash
codeplane run "Explain how closures work in JavaScript"
```

---

### tui

بدء واجهة المستخدم terminal (TUI) الخاصة بـ CodePlane.

```bash
codeplane [project]
```

#### الخيارات

| الخيار       | المختصر | الوصف                                                             |
| ------------ | ------- | ----------------------------------------------------------------- |
| `--continue` | `-c`    | متابعة الجلسة الأخيرة                                             |
| `--session`  | `-s`    | معرّف الجلسة للمتابعة                                             |
| `--fork`     |         | تفريع الجلسة عند المتابعة (يستخدم مع `--continue` أو `--session`) |
| `--prompt`   |         | الموجّه المراد استخدامه                                           |
| `--model`    | `-m`    | النموذج المراد استخدامه بصيغة provider/model                      |
| `--agent`    |         | الوكيل المراد استخدامه                                            |
| `--port`     |         | المنفذ الذي يتم الاستماع عليه                                     |
| `--hostname` |         | اسم المضيف الذي يتم الاستماع عليه                                 |

---

## الأوامر

يتضمن CLI الخاص بـ CodePlane أيضا الأوامر التالية.

---

### agent

إدارة وكلاء CodePlane.

```bash
codeplane agent [command]
```

---

### attach

إرفاق terminal بخادم الواجهة الخلفية لـ CodePlane قيد التشغيل بالفعل، والذي تم تشغيله عبر الأمرين `serve` أو `web`.

```bash
codeplane attach [url]
```

يتيح ذلك استخدام واجهة TUI مع واجهة خلفية لـ CodePlane تعمل عن بعد. على سبيل المثال:

```bash
# ابدأ خادم الواجهة الخلفية للوصول عبر الويب/الجوال
codeplane web --port 4096 --hostname 0.0.0.0

# في محطة طرفية (terminal) أخرى، اربط TUI بالواجهة الخلفية قيد التشغيل
codeplane attach http://10.20.30.40:4096
```

#### الرايات

| الراية      | المختصر | الوصف                               |
| ----------- | ------- | ----------------------------------- |
| `--dir`     |         | دليل العمل الذي ستبدأ منه واجهة TUI |
| `--session` | `-s`    | معرّف الجلسة للمتابعة               |

---

#### create

إنشاء وكيل جديد بإعدادات مخصصة.

```bash
codeplane agent create
```

سيرشدك هذا الأمر خلال إنشاء وكيل جديد مع موجه نظام مخصص وإعدادات الأدوات.

---

#### list

عرض جميع الوكلاء المتاحين.

```bash
codeplane agent list
```

---

### auth

أمر لإدارة بيانات الاعتماد وتسجيل الدخول لمزودي الخدمة.

```bash
codeplane auth [command]
```

---

#### login

يعتمد CodePlane على قائمة المزودين في [Models.dev](https://models.dev)، لذا يمكنك استخدام `codeplane auth login` لتهيئة مفاتيح API لأي مزود ترغب باستخدامه. يتم حفظ ذلك في `~/.local/share/codeplane/auth.json`.

```bash
codeplane auth login
```

عند تشغيل CodePlane يقوم بتحميل المزودين من ملف بيانات الاعتماد، وكذلك أي مفاتيح معرّفة في متغيرات البيئة لديك أو في ملف `.env` ضمن مشروعك.

---

#### list

يسرد جميع المزودين الذين تم توثيقهم كما هم محفوظون في ملف بيانات الاعتماد.

```bash
codeplane auth list
```

أو النسخة المختصرة.

```bash
codeplane auth ls
```

---

#### logout

يسجلك خارج مزود عبر حذفه من ملف بيانات الاعتماد.

```bash
codeplane auth logout
```

---

### github

إدارة وكيل GitHub لأتمتة المستودع.

```bash
codeplane github [command]
```

---

#### install

تثبيت وكيل GitHub في مستودعك.

```bash
codeplane github install
```

يقوم ذلك بإعداد سير عمل GitHub Actions اللازم ويرشدك خلال عملية التهيئة. [اعرف المزيد](/docs/github).

---

#### run

تشغيل وكيل GitHub. يُستخدم هذا عادة ضمن GitHub Actions.

```bash
codeplane github run
```

##### الرايات

| الراية    | الوصف                                |
| --------- | ------------------------------------ |
| `--event` | حدث GitHub مُحاكى لتشغيل الوكيل عليه |
| `--token` | رمز وصول شخصي لـ GitHub              |

---

### mcp

إدارة خوادم Model Context Protocol.

```bash
codeplane mcp [command]
```

---

#### add

إضافة خادم MCP إلى تهيئتك.

```bash
codeplane mcp add
```

سيرشدك هذا الأمر خلال إضافة خادم MCP محلي أو بعيد.

---

#### list

عرض جميع خوادم MCP المُهيأة وحالة اتصالها.

```bash
codeplane mcp list
```

أو استخدم النسخة المختصرة.

```bash
codeplane mcp ls
```

---

#### auth

إجراء المصادقة مع خادم MCP يدعم OAuth.

```bash
codeplane mcp auth [name]
```

إذا لم تُحدِّد اسم خادم، فسيُطلب منك الاختيار من الخوادم المتاحة الداعمة لـ OAuth.

يمكنك أيضا عرض الخوادم الداعمة لـ OAuth وحالة المصادقة الخاصة بها.

```bash
codeplane mcp auth list
```

أو استخدم النسخة المختصرة.

```bash
codeplane mcp auth ls
```

---

#### logout

إزالة بيانات اعتماد OAuth لخادم MCP.

```bash
codeplane mcp logout [name]
```

---

#### debug

تشخيص مشاكل اتصال OAuth لخادم MCP.

```bash
codeplane mcp debug <name>
```

---

### models

عرض جميع النماذج المتاحة من المزودين المُهيأين.

```bash
codeplane models [provider]
```

يعرض هذا الأمر جميع النماذج المتاحة عبر المزودين المُهيأين لديك بصيغة `provider/model`.

هذا مفيد لتحديد اسم النموذج الدقيق الذي ستستخدمه في [تهيئتك](/docs/config/).

يمكنك اختياريا تمرير معرّف مزود لتصفية النماذج حسب ذلك المزود.

```bash
codeplane models anthropic
```

#### الرايات

| الراية      | الوصف                                                         |
| ----------- | ------------------------------------------------------------- |
| `--refresh` | تحديث ذاكرة التخزين المؤقت للنماذج من models.dev              |
| `--verbose` | استخدام مخرجات أكثر تفصيلا للنماذج (تشمل بيانات مثل التكاليف) |

استخدم الراية `--refresh` لتحديث قائمة النماذج المخزنة مؤقتا. يفيد ذلك عند إضافة نماذج جديدة إلى مزود وتريد رؤيتها في CodePlane.

```bash
codeplane models --refresh
```

---

### run

تشغيل codeplane في وضع غير تفاعلي عبر تمرير موجه مباشرة.

```bash
codeplane run [message..]
```

هذا مفيد للبرمجة النصية والأتمتة، أو عندما تريد إجابة سريعة دون تشغيل واجهة TUI كاملة. على سبيل المثال.

```bash "codeplane run"
codeplane run Explain the use of context in Go
```

يمكنك أيضا الإرفاق بمثيل `codeplane serve` قيد التشغيل لتجنّب زمن الإقلاع البارد لخوادم MCP في كل تشغيل:

```bash
# ابدأ خادمًا بلا واجهة في محطة طرفية واحدة
codeplane serve

# في محطة طرفية أخرى، شغّل الأوامر التي ترتبط به
codeplane run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### الرايات

| الراية       | المختصر | الوصف                                                             |
| ------------ | ------- | ----------------------------------------------------------------- |
| `--command`  |         | الأمر المراد تشغيله؛ استخدم الرسالة كوسائط                        |
| `--continue` | `-c`    | متابعة الجلسة الأخيرة                                             |
| `--session`  | `-s`    | معرّف الجلسة للمتابعة                                             |
| `--fork`     |         | تفريع الجلسة عند المتابعة (يستخدم مع `--continue` أو `--session`) |
| `--share`    |         | مشاركة الجلسة                                                     |
| `--model`    | `-m`    | النموذج المراد استخدامه بصيغة provider/model                      |
| `--agent`    |         | الوكيل المراد استخدامه                                            |
| `--file`     | `-f`    | ملف/ملفات لإرفاقها بالرسالة                                       |
| `--format`   |         | التنسيق: default (منسق) أو json (أحداث JSON خام)                  |
| `--title`    |         | عنوان للجلسة (يستخدم موجهًا مقتطعًا إن لم تُحدَّد قيمة)           |
| `--attach`   |         | الإرفاق بخادم codeplane قيد التشغيل (مثل http://localhost:4096)    |
| `--port`     |         | منفذ الخادم المحلي (الافتراضي منفذ عشوائي)                        |

---

### serve

بدء خادم CodePlane بدون واجهة للوصول عبر API. راجع [توثيق الخادم](/docs/server) لواجهة HTTP الكاملة.

```bash
codeplane serve
```

يشغّل هذا خادم HTTP يوفّر وصولا عبر API لوظائف codeplane دون واجهة TUI. اضبط `CODEPLANE_SERVER_PASSWORD` لتفعيل مصادقة HTTP الأساسية (اسم المستخدم الافتراضي `codeplane`).

#### الرايات

| الراية       | الوصف                                     |
| ------------ | ----------------------------------------- |
| `--port`     | المنفذ الذي يتم الاستماع عليه             |
| `--hostname` | اسم المضيف الذي يتم الاستماع عليه         |
| `--mdns`     | تفعيل اكتشاف mDNS                         |
| `--cors`     | أصول/منشأات إضافية للمتصفح للسماح بـ CORS |

---

### session

إدارة جلسات CodePlane.

```bash
codeplane session [command]
```

---

#### list

عرض جميع جلسات CodePlane.

```bash
codeplane session list
```

##### الرايات

| الراية        | المختصر | الوصف                                 |
| ------------- | ------- | ------------------------------------- |
| `--max-count` | `-n`    | حصر النتائج في أحدث N جلسات           |
| `--format`    |         | تنسيق المخرجات: table أو json (table) |

---

### stats

عرض إحصاءات استخدام الرموز والتكلفة لجلسات CodePlane لديك.

```bash
codeplane stats
```

#### الرايات

| الراية      | الوصف                                                                     |
| ----------- | ------------------------------------------------------------------------- |
| `--days`    | عرض الإحصاءات لآخر N يومًا (الافتراضي: كل الوقت)                          |
| `--tools`   | عدد الأدوات المطلوب عرضها (الافتراضي: الكل)                               |
| `--models`  | عرض تفصيل استخدام النماذج (مخفي افتراضيا). مرّر رقمًا لعرض أعلى N         |
| `--project` | التصفية حسب المشروع (الافتراضي: كل المشاريع، سلسلة فارغة: المشروع الحالي) |

---

### export

تصدير بيانات الجلسة بصيغة JSON.

```bash
codeplane export [sessionID]
```

إذا لم تُقدّم معرّف جلسة، فسيُطلب منك الاختيار من الجلسات المتاحة.

---

### import

استيراد بيانات الجلسة من ملف JSON أو رابط مشاركة CodePlane.

```bash
codeplane import <file>
```

يمكنك الاستيراد من ملف محلي أو من رابط مشاركة CodePlane.

```bash
codeplane import session.json
codeplane import https://opncd.ai/s/abc123
```

---

### web

بدء خادم CodePlane بدون واجهة مع واجهة ويب.

```bash
codeplane web
```

يشغّل هذا خادم HTTP ويفتح متصفح ويب للوصول إلى CodePlane عبر واجهة ويب. اضبط `CODEPLANE_SERVER_PASSWORD` لتفعيل مصادقة HTTP الأساسية (اسم المستخدم الافتراضي `codeplane`).

#### الرايات

| الراية       | الوصف                                     |
| ------------ | ----------------------------------------- |
| `--port`     | المنفذ الذي يتم الاستماع عليه             |
| `--hostname` | اسم المضيف الذي يتم الاستماع عليه         |
| `--mdns`     | تفعيل اكتشاف mDNS                         |
| `--cors`     | أصول/منشأات إضافية للمتصفح للسماح بـ CORS |

---

### acp

بدء خادم ACP (Agent Client Protocol).

```bash
codeplane acp
```

يشغّل هذا الأمر خادم ACP يتواصل عبر stdin/stdout باستخدام nd-JSON.

#### الرايات

| الراية       | الوصف                             |
| ------------ | --------------------------------- |
| `--cwd`      | دليل العمل                        |
| `--port`     | المنفذ الذي يتم الاستماع عليه     |
| `--hostname` | اسم المضيف الذي يتم الاستماع عليه |

---

### uninstall

إلغاء تثبيت CodePlane وإزالة جميع الملفات ذات الصلة.

```bash
codeplane uninstall
```

#### الرايات

| الراية          | المختصر | الوصف                               |
| --------------- | ------- | ----------------------------------- |
| `--keep-config` | `-c`    | الإبقاء على ملفات التهيئة           |
| `--keep-data`   | `-d`    | الإبقاء على بيانات الجلسات واللقطات |
| `--dry-run`     |         | عرض ما سيتم حذفه دون تنفيذ الحذف    |
| `--force`       | `-f`    | تخطي مطالبات التأكيد                |

---

### upgrade

تحديث codeplane إلى أحدث إصدار أو إلى إصدار محدد.

```bash
codeplane upgrade [target]
```

للترقية إلى أحدث إصدار.

```bash
codeplane upgrade
```

للترقية إلى إصدار محدد.

```bash
codeplane upgrade v0.1.48
```

#### الرايات

| الراية     | المختصر | الوصف                                                       |
| ---------- | ------- | ----------------------------------------------------------- |
| `--method` | `-m`    | طريقة التثبيت المستخدمة: curl أو npm أو pnpm أو bun أو brew |

---

## الخيارات العامة

يدعم سطر أوامر codeplane الخيارات العامة التالية.

| الراية         | المختصر | الوصف                                  |
| -------------- | ------- | -------------------------------------- |
| `--help`       | `-h`    | عرض المساعدة                           |
| `--version`    | `-v`    | طباعة رقم الإصدار                      |
| `--print-logs` |         | طباعة السجلات إلى stderr               |
| `--log-level`  |         | مستوى السجل (DEBUG, INFO, WARN, ERROR) |

---

## متغيرات البيئة

يمكن تهيئة CodePlane باستخدام متغيرات البيئة.

| المتغير                               | النوع   | الوصف                                                     |
| ------------------------------------- | ------- | --------------------------------------------------------- |
| `CODEPLANE_AUTO_SHARE`                 | boolean | مشاركة الجلسات تلقائيا                                    |
| `CODEPLANE_GIT_BASH_PATH`              | string  | مسار ملف Git Bash التنفيذي على Windows                    |
| `CODEPLANE_CONFIG`                     | string  | مسار ملف التهيئة                                          |
| `CODEPLANE_CONFIG_DIR`                 | string  | مسار دليل التهيئة                                         |
| `CODEPLANE_CONFIG_CONTENT`             | string  | محتوى تهيئة JSON مُضمّن                                   |
| `CODEPLANE_DISABLE_AUTOUPDATE`         | boolean | تعطيل التحقق التلقائي من التحديثات                        |
| `CODEPLANE_DISABLE_PRUNE`              | boolean | تعطيل تنقية البيانات القديمة                              |
| `CODEPLANE_DISABLE_TERMINAL_TITLE`     | boolean | تعطيل تحديث عنوان terminal تلقائيا                        |
| `CODEPLANE_PERMISSION`                 | string  | تهيئة أذونات JSON مُضمّنة                                 |
| `CODEPLANE_DISABLE_DEFAULT_PLUGINS`    | boolean | تعطيل الإضافات الافتراضية                                 |
| `CODEPLANE_DISABLE_LSP_DOWNLOAD`       | boolean | تعطيل تنزيل خوادم LSP تلقائيا                             |
| `CODEPLANE_ENABLE_EXPERIMENTAL_MODELS` | boolean | تفعيل النماذج التجريبية                                   |
| `CODEPLANE_DISABLE_AUTOCOMPACT`        | boolean | تعطيل ضغط السياق تلقائيا                                  |
| `CODEPLANE_DISABLE_CLAUDE_CODE`        | boolean | تعطيل القراءة من `.claude` (الموجه + المهارات)            |
| `CODEPLANE_DISABLE_CLAUDE_CODE_PROMPT` | boolean | تعطيل قراءة `~/.claude/CLAUDE.md`                         |
| `CODEPLANE_DISABLE_CLAUDE_CODE_SKILLS` | boolean | تعطيل تحميل `.claude/skills`                              |
| `CODEPLANE_DISABLE_MODELS_FETCH`       | boolean | تعطيل جلب النماذج من مصادر بعيدة                          |
| `CODEPLANE_FAKE_VCS`                   | string  | مزود VCS وهمي لأغراض الاختبار                             |
| `CODEPLANE_CLIENT`                     | string  | معرّف العميل (الافتراضي `cli`)                            |
| `CODEPLANE_ENABLE_EXA`                 | boolean | تفعيل أدوات بحث الويب من Exa                              |
| `CODEPLANE_SERVER_PASSWORD`            | string  | تفعيل المصادقة الأساسية لخادمي `serve`/`web`              |
| `CODEPLANE_SERVER_USERNAME`            | string  | تجاوز اسم مستخدم المصادقة الأساسية (الافتراضي `codeplane`) |
| `CODEPLANE_MODELS_URL`                 | string  | رابط مخصص لجلب تهيئة النماذج                              |

---

### تجريبي

تقوم متغيرات البيئة التالية بتفعيل ميزات تجريبية قد تتغير أو تتم إزالتها.

| المتغير                                         | النوع   | الوصف                                       |
| ----------------------------------------------- | ------- | ------------------------------------------- |
| `CODEPLANE_EXPERIMENTAL`                         | boolean | تفعيل جميع الميزات التجريبية                |
| `CODEPLANE_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | تفعيل اكتشاف الأيقونات                      |
| `CODEPLANE_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | تعطيل النسخ عند التحديد في TUI              |
| `CODEPLANE_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | المهلة الافتراضية لأوامر bash بالميلي ثانية |
| `CODEPLANE_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | الحد الأقصى لرموز المخرجات لاستجابات LLM    |
| `CODEPLANE_EXPERIMENTAL_FILEWATCHER`             | boolean | تفعيل مراقب الملفات لكامل الدليل            |
| `CODEPLANE_EXPERIMENTAL_OXFMT`                   | boolean | تفعيل مُنسّق oxfmt                          |
| `CODEPLANE_EXPERIMENTAL_LSP_TOOL`                | boolean | تفعيل أداة LSP تجريبية                      |
| `CODEPLANE_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | تعطيل مراقب الملفات                         |
| `CODEPLANE_EXPERIMENTAL_EXA`                     | boolean | تفعيل ميزات Exa التجريبية                   |
| `CODEPLANE_EXPERIMENTAL_LSP_TY`                  | boolean | تمكين TY LSP لملفات python                  |
| `CODEPLANE_EXPERIMENTAL_MARKDOWN`                | boolean | تفعيل ميزات markdown تجريبية                |
| `CODEPLANE_EXPERIMENTAL_PLAN_MODE`               | boolean | تفعيل وضع الخطة                             |
