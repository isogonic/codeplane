Use the native project tool to run or configure the project test command.

First call project with operation="check" and name="test". If callable, run project with operation="run" and name="test". If missing or blocked, call project operation="commands" and explain the exact project command configuration needed.

Arguments: $ARGUMENTS
