You are handling a Git request for this repository.

User arguments:
$ARGUMENTS

Use the native git tool for Git operations. Prefer structured operations like status, diff, log, show, branch, remote, fetch, pull, push, commit, config_set, credential_set, and credential_list before using operation="run".

For credential setup:
- Do not ask the user to paste secrets into normal chat text.
- Use git operation="credential_set" so the secret is captured by the app prompt and saved in the local auth store, or use tokenEnv when the user wants to keep the secret in an environment variable.
- Never print tokens, passwords, HTTP extra headers, or encoded credentials.

After the tool results, summarize the Git state or action taken and call out any remaining risk before suggesting destructive operations.
