<svg width="512" height="512" viewBox="0 0 512 512" xmlns="http://www.w3.org/2000/svg">
  <!-- Favicon only: a rounded white tile with the Codeplane paper-plane mark.
       White background on purpose so it reads on both light and dark browser
       chrome and in Google's search results. The in-app logo stays the bare
       plane (components/logo.tsx) — this tile is icon-only. -->
  <rect width="512" height="512" rx="116" fill="#ffffff"/>
  <path
    d="M64 64L448 256L64 448V320L256 256L64 192V64Z"
    fill="#1a1a1a"
    transform="translate(256 256) scale(0.58) translate(-256 -256)"
  />
</svg>
